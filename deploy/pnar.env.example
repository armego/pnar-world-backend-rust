# Minimal example env file for PNAR service (place at /etc/pnar/pnar.env)
# DO NOT commit secrets. Use a secure method to store JWT_SECRET and DB credentials.

SERVER_HOST=127.0.0.1
SERVER_PORT=8000
RUST_LOG=info
DATABASE_URL=postgresql://pnar_user:REPLACE_PASSWORD@127.0.0.1:5432/pnar_prod
JWT_SECRET=REPLACE_WITH_STRONG_SECRET
